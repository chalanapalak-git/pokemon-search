export interface SchemaValue<T> {
    validate(value: T): string | null;
    default: T | undefined;
    required: boolean;
}
export type SchemaDefinition<T extends object> = {
    [key in keyof T]: SchemaValue<T[key]>;
};
export declare class SchemaValidationError extends Error {
    constructor(message: string);
}
export declare class Schema<Values extends object> {
    private definition;
    constructor(definition: SchemaDefinition<Values>);
    validate(values?: Partial<Values>, message?: string): Partial<Values>;
    private get default();
}
type InferValuesFromSchema<T> = T extends Schema<infer P> ? P : never;
export type SchemaValues<Schema, RequiredValues extends keyof InferValuesFromSchema<Schema> = never> = Partial<InferValuesFromSchema<Schema>> & Pick<InferValuesFromSchema<Schema>, RequiredValues>;
export {};
