import type { SchemaValue } from '../schema.js';
import { type ValueConfig } from './value.js';
export interface EnumValueConfig<T> extends ValueConfig<T> {
    enum: Record<string | number, string | number>;
}
export declare class EnumValue<T> implements SchemaValue<T> {
    private config;
    private value;
    constructor(config: EnumValueConfig<T>);
    validate(value: unknown): "value is required." | "value is not in enum." | null;
    get default(): T | undefined;
    get required(): boolean;
}
