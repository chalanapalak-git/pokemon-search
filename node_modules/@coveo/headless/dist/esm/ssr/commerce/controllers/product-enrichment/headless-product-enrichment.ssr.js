import { buildProductEnrichment, } from '../../../../controllers/commerce/product-enrichment/headless-product-enrichment.js';
import { MissingControllerProps } from '../../../common/errors.js';
import { createControllerWithKind, Kind } from '../../types/kind.js';
/**
 * Defines the `ProductEnrichment` controller for the purpose of server-side rendering.
 * @group Definers
 *
 * @param props - The configurable `ProductEnrichment` properties.
 * @returns The `ProductEnrichment` controller definition.
 */
export function defineProductEnrichment() {
    return {
        standalone: true,
        buildWithProps: (engine, props) => {
            if (props === undefined) {
                throw new MissingControllerProps(Kind.ProductEnrichment);
            }
            const controller = buildProductEnrichment(engine, props);
            return createControllerWithKind(controller, Kind.ProductEnrichment);
        },
    };
}
