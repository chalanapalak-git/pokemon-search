import { buildPager, } from '../../../../controllers/pager/headless-pager.js';
export { buildPager } from '../../../../controllers/pager/headless-pager.js';
/**
 * Defines a `Pager` controller instance.
 * @group Definers
 *
 * @param props - The configurable `Pager` properties.
 * @returns The `Pager` controller definition.
 * */
export function definePager(props) {
    return {
        build: (engine) => buildPager(engine, props),
    };
}
