import { buildExecuteTrigger, } from '../../../../controllers/triggers/headless-execute-trigger.js';
export { buildExecuteTrigger } from '../../../../controllers/triggers/headless-execute-trigger.js';
/**
 * Defines an `ExecuteTrigger` controller instance.
 * @group Definers
 *
 * @returns The `ExecuteTrigger` controller definition.
 * */
export function defineExecuteTrigger() {
    return {
        build: (engine) => buildExecuteTrigger(engine),
    };
}
