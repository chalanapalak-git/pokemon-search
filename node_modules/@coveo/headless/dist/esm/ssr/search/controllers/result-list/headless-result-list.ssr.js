import { buildResultList, } from '../../../../controllers/result-list/headless-result-list.js';
export { buildResultList } from '../../../../controllers/result-list/headless-result-list.js';
/**
 * Defines a `ResultList` controller instance.
 * @group Definers
 *
 * @param props - The configurable `ResultList` properties.
 * @returns The `ResultList` controller definition.
 * */
export function defineResultList(props) {
    return {
        build: (engine) => buildResultList(engine, props),
    };
}
