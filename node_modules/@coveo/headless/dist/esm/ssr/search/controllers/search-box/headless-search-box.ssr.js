import { buildSearchBox, } from '../../../../controllers/search-box/headless-search-box.js';
export { buildSearchBox } from '../../../../controllers/search-box/headless-search-box.js';
/**
 * Defines a `SearchBox` controller instance.
 * @group Definers
 *
 * @param props - The configurable `SearchBox` properties.
 * @returns The `SearchBox` controller definition.
 * */
export function defineSearchBox(props) {
    return {
        build: (engine) => buildSearchBox(engine, props),
    };
}
