import { buildQueryError, } from '../../../../controllers/query-error/headless-query-error.js';
export { buildQueryError } from '../../../../controllers/query-error/headless-query-error.js';
/**
 * Defines a `QueryError` controller instance.
 * @group Definers
 *
 * @returns The `QueryError` controller definition.
 * */
export function defineQueryError() {
    return {
        build: (engine) => buildQueryError(engine),
    };
}
