import { buildDateFilter, } from '../../../../../../controllers/facets/range-facet/date-facet/headless-date-filter.js';
export { buildDateFilter } from '../../../../../../controllers/facets/range-facet/date-facet/headless-date-filter.js';
/**
 * Defines a `DateFilter` controller instance.
 * @group Definers
 *
 * @param props - The configurable `DateFilter` properties.
 * @returns The `DateFilter` controller definition.
 * */
export function defineDateFilter(props) {
    return {
        build: (engine) => buildDateFilter(engine, props),
    };
}
