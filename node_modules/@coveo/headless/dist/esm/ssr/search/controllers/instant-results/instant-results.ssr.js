import { buildInstantResults, } from '../../../../controllers/instant-results/instant-results.js';
export { buildInstantResults } from '../../../../controllers/instant-results/instant-results.js';
/**
 * Defines an `InstantResults` controller instance.
 * @group Definers
 *
 * @param props - The configurable `InstantResults` properties.
 * @returns The `InstantResults` controller definition.
 * */
export function defineInstantResults(props) {
    return {
        build: (engine) => buildInstantResults(engine, props),
    };
}
