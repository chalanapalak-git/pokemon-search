import { buildSearchStatus, } from '../../../../controllers/search-status/headless-search-status.js';
export { buildSearchStatus } from '../../../../controllers/search-status/headless-search-status.js';
/**
 * Defines a `SearchStatus` controller instance.
 * @group Definers
 *
 * @returns The `SearchStatus` controller definition.
 * */
export function defineSearchStatus() {
    return {
        build: (engine) => buildSearchStatus(engine),
    };
}
