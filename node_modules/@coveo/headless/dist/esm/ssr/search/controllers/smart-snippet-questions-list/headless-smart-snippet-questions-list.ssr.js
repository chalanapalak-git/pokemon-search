import { buildSmartSnippetQuestionsList, } from '../../../../controllers/smart-snippet-questions-list/headless-smart-snippet-questions-list.js';
export { buildSmartSnippetQuestionsList } from '../../../../controllers/smart-snippet-questions-list/headless-smart-snippet-questions-list.js';
/**
 * Defines a `SmartSnippetQuestionsList` controller instance.
 * @group Definers
 *
 * @param props - The configurable `SmartSnippetQuestionsList` properties.
 * @returns The `SmartSnippetQuestionsList` controller definition.
 * */
export function defineSmartSnippetQuestionsList(props) {
    return {
        build: (engine) => buildSmartSnippetQuestionsList(engine, props),
    };
}
