import { buildFacetManager, } from '../../../../controllers/facet-manager/headless-facet-manager.js';
export { buildFacetManager } from '../../../../controllers/facet-manager/headless-facet-manager.js';
/**
 * Defines a `FacetManager` controller instance.
 * @group Definers
 *
 * @returns The `FacetManager` controller definition.
 * */
export function defineFacetManager() {
    return {
        build: (engine) => buildFacetManager(engine),
    };
}
