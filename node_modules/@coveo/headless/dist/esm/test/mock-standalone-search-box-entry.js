export function buildMockStandaloneSearchBoxEntry(config = {}) {
    return {
        analytics: {
            cause: '',
            metadata: null,
        },
        defaultRedirectionUrl: '',
        isLoading: false,
        redirectTo: '',
        ...config,
    };
}
