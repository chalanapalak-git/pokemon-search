import { logSearchboxSubmit } from '../features/query/query-analytics-actions.js';
import { buildMockSearchResponse } from './mock-search-response.js';
export function buildMockSearch(config = {}) {
    return {
        response: buildMockSearchResponse(),
        duration: 0,
        queryExecuted: '',
        error: null,
        automaticallyCorrected: false,
        originalQuery: '',
        ...config,
    };
}
export function buildMockLegacySearch(config = {}) {
    return {
        response: buildMockSearchResponse(),
        duration: 0,
        queryExecuted: '',
        error: null,
        automaticallyCorrected: false,
        originalQuery: '',
        analyticsAction: logSearchboxSubmit(),
        ...config,
    };
}
