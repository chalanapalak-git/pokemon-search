export function buildMockPagination(config = {}) {
    return {
        firstResult: 0,
        defaultNumberOfResults: 0,
        numberOfResults: 0,
        totalCountFiltered: 0,
        ...config,
    };
}
