import { buildMockFacetRequest } from './mock-facet-request.js';
export function buildMockFacetSlice(config = {}) {
    return {
        request: buildMockFacetRequest(),
        hasBreadcrumbs: true,
        ...config,
    };
}
