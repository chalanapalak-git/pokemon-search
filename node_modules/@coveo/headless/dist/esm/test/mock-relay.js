import { buildMockMeta } from './mock-meta.js';
export function buildMockRelay(config = {}) {
    return {
        emit: vi.fn(),
        getMeta: vi.fn((_) => buildMockMeta({})),
        on: vi.fn(),
        off: vi.fn(),
        updateConfig: vi.fn(),
        version: 'foo',
        ...config,
    };
}
