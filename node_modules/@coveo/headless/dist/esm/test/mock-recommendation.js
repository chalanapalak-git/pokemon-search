import { logInterfaceLoad } from '../features/analytics/analytics-actions.js';
export function buildMockRecommendation(config = {}) {
    return {
        recommendations: [],
        duration: 0,
        error: null,
        analyticsAction: logInterfaceLoad(),
        searchUid: '123',
        splitTestRun: '',
        pipeline: '',
        ...config,
    };
}
