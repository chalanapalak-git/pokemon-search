import { buildMockResult } from './mock-result.js';
export function buildMockResultWithFolding(config = {}) {
    return {
        ...buildMockResult(config),
        parentResult: null,
        childResults: [],
        totalNumberOfChildResults: 1,
        ...config,
    };
}
