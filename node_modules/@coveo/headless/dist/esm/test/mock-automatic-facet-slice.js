import { buildMockAutomaticFacetResponse } from './mock-automatic-facet-response.js';
export function buildMockAutomaticFacetSlice(config = {}) {
    return {
        response: buildMockAutomaticFacetResponse(),
        ...config,
    };
}
