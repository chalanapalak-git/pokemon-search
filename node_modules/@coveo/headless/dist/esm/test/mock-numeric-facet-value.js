export function buildMockNumericFacetValue(config = {}) {
    return {
        start: 0,
        end: 10,
        endInclusive: false,
        numberOfResults: 0,
        state: 'idle',
        ...config,
    };
}
