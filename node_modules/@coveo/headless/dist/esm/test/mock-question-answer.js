export function buildMockQuestionAnswer(config = {}) {
    return {
        answerSnippet: '',
        documentId: {
            contentIdKey: '',
            contentIdValue: '',
        },
        question: '',
        score: 0,
        ...config,
    };
}
export function buildMockQuestionsAnswers(config = {}) {
    return {
        ...buildMockQuestionAnswer(),
        relatedQuestions: [],
        ...config,
    };
}
