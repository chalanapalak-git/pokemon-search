export function buildMockDateFacetRequest(config = {}) {
    return {
        facetId: '',
        field: '',
        currentValues: [],
        filterFacetCount: false,
        generateAutomaticRanges: false,
        injectionDepth: 1000,
        numberOfValues: 8,
        preventAutoSelect: false,
        sortCriteria: 'ascending',
        resultsMustMatch: 'atLeastOneValue',
        type: 'dateRange',
        rangeAlgorithm: 'even',
        ...config,
    };
}
