import { buildLogger } from '../app/logger.js';
export function buildMockThunkExtraArguments(config = {}) {
    return {
        analyticsClientMiddleware: vi.fn(),
        preprocessRequest: vi.fn(),
        validatePayload: vi.fn(),
        logger: buildLogger({ level: 'silent' }),
        ...config,
    };
}
