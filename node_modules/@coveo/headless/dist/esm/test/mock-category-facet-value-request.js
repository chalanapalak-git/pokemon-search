export function buildMockCategoryFacetValueRequest(config = {}) {
    return {
        value: '',
        state: 'idle',
        children: [],
        retrieveChildren: false,
        retrieveCount: 0,
        ...config,
    };
}
