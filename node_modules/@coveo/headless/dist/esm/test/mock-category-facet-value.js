export function buildMockCategoryFacetValue(config = {}) {
    return {
        children: [],
        numberOfResults: 0,
        path: [],
        state: 'idle',
        value: '',
        moreValuesAvailable: false,
        isLeafValue: false,
        ...config,
    };
}
