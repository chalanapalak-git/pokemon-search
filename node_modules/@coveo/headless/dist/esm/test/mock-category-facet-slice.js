import { buildMockCategoryFacetRequest } from './mock-category-facet-request.js';
export function buildMockCategoryFacetSlice(config = {}) {
    return {
        request: buildMockCategoryFacetRequest(),
        initialNumberOfValues: 0,
        ...config,
    };
}
