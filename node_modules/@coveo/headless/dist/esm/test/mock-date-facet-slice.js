import { buildMockDateFacetRequest } from './mock-date-facet-request.js';
export function buildMockDateFacetSlice(config = {}) {
    return {
        request: buildMockDateFacetRequest(),
        ...config,
    };
}
