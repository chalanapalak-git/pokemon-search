import { logFacetDeselect, logFacetSelect, } from '../../facet-set/facet-set-insight-analytics-actions.js';
import { isRangeFacetValueSelected } from './range-facet-utils.js';
export const getInsightAnalyticsActionForToggleRangeFacetSelect = (facetId, selection) => {
    const facetValue = `${selection.start}..${selection.end}`;
    const payload = { facetId, facetValue };
    return isRangeFacetValueSelected(selection)
        ? logFacetDeselect(payload)
        : logFacetSelect(payload);
};
