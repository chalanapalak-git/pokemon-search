function isFacetResponse(state, response) {
    return !!response && response.facetId in state.facetSet;
}
export const baseFacetResponseSelector = (state, id) => state.search?.response.facets.find((response) => response.facetId === id);
export const facetRequestSelector = (state, id) => state.facetSet[id]?.request;
export const facetResponseSelector = (state, facetId) => {
    const response = baseFacetResponseSelector(state, facetId);
    return isFacetResponse(state, response) ? response : undefined;
};
export const facetResponseSelectedValuesSelector = (state, facetId) => facetResponseSelector(state, facetId)?.values.filter((value) => value.state === 'selected') || [];
export const facetResponseActiveValuesSelector = (state, facetId) => facetResponseSelector(state, facetId)?.values.filter((value) => value.state !== 'idle') || [];
export const isFacetLoadingResponseSelector = (state) => state.search.isLoading;
