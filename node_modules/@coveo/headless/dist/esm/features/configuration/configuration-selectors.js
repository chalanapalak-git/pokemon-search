export const selectLocale = (state) => state.configuration.search.locale;
export const selectTimezone = (state) => state.configuration.search.timezone;
/**
 * Selects the agent ID from the knowledge configuration.
 * Used for agent-based answer generation via the Agent API.
 */
export const selectAgentId = (state) => state.configuration?.knowledge?.agentId;
