export const constructGenerateFollowUpAnswerParams = (followUpQuestion, state) => {
    const conversationId = state.followUpAnswers?.conversationId ?? '';
    return {
        conversationId,
        q: followUpQuestion,
    };
};
