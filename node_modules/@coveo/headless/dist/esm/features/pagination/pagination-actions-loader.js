import { paginationReducer as pagination } from '../../features/pagination/pagination-slice.js';
import { nextPage, previousPage, registerNumberOfResults, registerPage, updateNumberOfResults, updatePage, } from './pagination-actions.js';
/**
 * Loads the `pagination` reducer and returns possible action creators.
 *
 * @param engine - The headless engine.
 * @returns An object holding the action creators.
 *
 * @group Actions
 * @category Pagination
 */
export function loadPaginationActions(engine) {
    engine.addReducers({ pagination });
    return {
        nextPage,
        previousPage,
        registerNumberOfResults,
        registerPage,
        updateNumberOfResults,
        updatePage,
    };
}
