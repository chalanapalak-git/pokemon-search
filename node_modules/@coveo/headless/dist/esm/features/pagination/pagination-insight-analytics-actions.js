import { makeInsightAnalyticsActionFactory, } from '../analytics/analytics-utils.js';
import { LegacySearchPageEvents } from '../analytics/legacy-search-action-cause.js';
import { getCaseContextAnalyticsMetadata } from '../case-context/case-context-state.js';
import { currentPageSelector } from './pagination-selectors.js';
export const logPageNumber = () => makeInsightAnalyticsActionFactory(LegacySearchPageEvents.pagerNumber)('analytics/pager/number', (client, state) => client.logPagerNumber({
    pagerNumber: currentPageSelector(state),
    ...getCaseContextAnalyticsMetadata(state.insightCaseContext),
}));
export const logPageNext = () => makeInsightAnalyticsActionFactory(LegacySearchPageEvents.pagerNext)('analytics/pager/next', (client, state) => client.logPagerNext({
    pagerNumber: currentPageSelector(state),
    ...getCaseContextAnalyticsMetadata(state.insightCaseContext),
}));
export const logPagePrevious = () => makeInsightAnalyticsActionFactory(LegacySearchPageEvents.pagerPrevious)('analytics/pager/previous', (client, state) => client.logPagerPrevious({
    pagerNumber: currentPageSelector(state),
    ...getCaseContextAnalyticsMetadata(state.insightCaseContext),
}));
