import { caseFieldReducer as caseField } from '../../features/case-field/case-field-slice.js';
import { fetchCaseClassifications, registerCaseField, updateCaseField, } from './case-field-actions.js';
/**
 * Loads the `case fields` reducer and returns possible action creators.
 *
 * @param engine - The headless engine.
 * @returns An object holding the action creators.
 *
 * @group Actions
 * @category CaseField
 */
export function loadCaseFieldActions(engine) {
    engine.addReducers({ caseField });
    return {
        registerCaseField,
        updateCaseField,
        fetchCaseClassifications,
    };
}
