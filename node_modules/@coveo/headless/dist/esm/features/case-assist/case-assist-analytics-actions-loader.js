import { logAbandonCase, logCaseNextStage, logCaseStart, logClassificationClick, logCreateCase, logDocumentSuggestionClick, logDocumentSuggestionRating, logQuickviewDocumentSuggestionClick, logSolveCase, logUpdateCaseField, } from './case-assist-analytics-actions.js';
/**
 * Loads the case assist analytics actions.
 * @param engine The case assist engine.
 * @returns The available analytics actions.
 *
 * @group Actions
 * @category Analytics
 */
export function loadCaseAssistAnalyticsActions(engine) {
    engine.addReducers({});
    return {
        logCaseStart,
        logCaseNextStage,
        logCreateCase,
        logSolveCase,
        logAbandonCase,
        logUpdateCaseField,
        logClassificationClick,
        logDocumentSuggestionClick,
        logDocumentSuggestionRating,
        logQuickviewDocumentSuggestionClick,
    };
}
