import { buildCoreFacetManager, } from '../../core/facet-manager/headless-core-facet-manager.js';
/**
 * Creates an insight `FacetManager` instance.
 *
 * @param engine - The insight engine.
 * @returns A `FacetManager` controller instance.
 *
 * @group Controllers
 * @category FacetManager
 */
export function buildFacetManager(engine) {
    return buildCoreFacetManager(engine);
}
