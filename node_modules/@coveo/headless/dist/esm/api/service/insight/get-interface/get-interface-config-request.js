import { baseInsightRequest } from '../insight-params.js';
export const buildGetInsightInterfaceConfigRequest = (req) => {
    return {
        ...baseInsightRequest(req, 'GET', 'application/json', '/interface'),
        requestParams: {},
    };
};
