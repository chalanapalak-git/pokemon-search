import { buildSearchBox, } from '../../../../controllers/commerce/search-box/headless-search-box.js';
/**
 * Defines a `SearchBox` controller instance.
 * @group Definers
 *
 * @returns The `SearchBox` controller definition.
 */
export function defineSearchBox(props = {}) {
    return {
        search: true,
        build: (engine) => buildSearchBox(engine, props),
    };
}
