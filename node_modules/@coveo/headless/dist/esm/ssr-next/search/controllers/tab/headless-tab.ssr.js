import { buildTab, } from '../../../../controllers/tab/headless-tab.js';
export { buildTab } from '../../../../controllers/tab/headless-tab.js';
/**
 * Defines a `Tab` controller instance.
 * @group Definers
 *
 * @param props - The configurable `Tab` properties.
 * @returns The `Tab` controller definition.
 * */
export function defineTab(props) {
    return {
        build: (engine) => buildTab(engine, props),
    };
}
