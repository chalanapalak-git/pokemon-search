import { buildFacet, } from '../../../../../controllers/facets/facet/headless-facet.js';
export { buildFacet } from '../../../../../controllers/facets/facet/headless-facet.js';
/**
 * Defines a `Facet` controller instance.
 * @group Definers
 *
 * @param props - The configurable `Facet` properties.
 * @returns The `Facet` controller definition.
 * */
export function defineFacet(props) {
    return {
        build: (engine) => buildFacet(engine, props),
    };
}
