import { buildResultsPerPage, } from '../../../../controllers/results-per-page/headless-results-per-page.js';
export { buildResultsPerPage } from '../../../../controllers/results-per-page/headless-results-per-page.js';
/**
 * Defines a `ResultsPerPage` controller instance.
 * @group Definers
 *
 * @param props - The configurable `ResultsPerPage` properties.
 * @returns The `ResultsPerPage` controller definition.
 * */
export function defineResultsPerPage(props) {
    return {
        build: (engine) => buildResultsPerPage(engine, props),
    };
}
