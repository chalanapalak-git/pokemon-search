import { buildTabManager, } from '../../../../controllers/tab-manager/headless-tab-manager.js';
export { buildTabManager } from '../../../../controllers/tab-manager/headless-tab-manager.js';
/**
 * Defines a `TabManager` controller instance.
 * @group Definers
 *
 * @returns The `TabManager` controller definition.
 * */
export function defineTabManager() {
    return {
        build: (engine) => buildTabManager(engine),
    };
}
