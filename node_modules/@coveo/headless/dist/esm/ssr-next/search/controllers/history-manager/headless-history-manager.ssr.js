import { buildHistoryManager, } from '../../../../controllers/history-manager/headless-history-manager.js';
export { buildHistoryManager } from '../../../../controllers/history-manager/headless-history-manager.js';
/**
 * Defines a `HistoryManager` controller instance.
 * @group Definers
 *
 * @returns The `HistoryManager` controller definition.
 * */
export function defineHistoryManager() {
    return {
        build: (engine) => buildHistoryManager(engine),
    };
}
