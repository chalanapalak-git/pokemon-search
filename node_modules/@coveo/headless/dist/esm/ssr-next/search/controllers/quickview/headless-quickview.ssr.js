import { buildQuickview, } from '../../../../controllers/quickview/headless-quickview.js';
export { buildQuickview } from '../../../../controllers/quickview/headless-quickview.js';
/**
 * Defines a `Quickview` controller instance.
 * @group Definers
 *
 * @param props - The configurable `Quickview` properties.
 * @returns The `Quickview` controller definition.
 * */
export function defineQuickview(props) {
    return {
        build: (engine) => buildQuickview(engine, props),
    };
}
