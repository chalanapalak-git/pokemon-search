import { buildStandaloneSearchBox, } from '../../../../controllers/standalone-search-box/headless-standalone-search-box.js';
export { buildStandaloneSearchBox } from '../../../../controllers/standalone-search-box/headless-standalone-search-box.js';
/**
 * Defines a `StandaloneSearchBox` controller instance.
 * @group Definers
 *
 * @param props - The configurable `StandaloneSearchBox` properties.
 * @returns The `StandaloneSearchBox` controller definition.
 * */
export function defineStandaloneSearchBox(props) {
    return {
        build: (engine) => buildStandaloneSearchBox(engine, props),
    };
}
