import { buildRecentQueriesList, } from '../../../../controllers/recent-queries-list/headless-recent-queries-list.js';
export { buildRecentQueriesList } from '../../../../controllers/recent-queries-list/headless-recent-queries-list.js';
/**
 * Defines a `RecentQueriesList` controller instance.
 * @group Definers
 *
 * @param props - The configurable `RecentQueriesList` properties.
 * @returns The `RecentQueriesList` controller definition.
 * */
export function defineRecentQueriesList(props) {
    return {
        build: (engine) => buildRecentQueriesList(engine, props),
    };
}
