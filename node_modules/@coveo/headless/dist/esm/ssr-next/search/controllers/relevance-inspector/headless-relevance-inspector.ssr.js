import { buildRelevanceInspector, } from '../../../../controllers/relevance-inspector/headless-relevance-inspector.js';
export { buildRelevanceInspector } from '../../../../controllers/relevance-inspector/headless-relevance-inspector.js';
/**
 * Defines a `RelevanceInspector` controller instance.
 * @group Definers
 *
 * @param props - The configurable `RelevanceInspector` properties.
 * @returns The `RelevanceInspector` controller definition.
 * */
export function defineRelevanceInspector(props) {
    return {
        build: (engine) => buildRelevanceInspector(engine, props),
    };
}
