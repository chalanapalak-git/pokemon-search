import { buildSmartSnippet, } from '../../../../controllers/smart-snippet/headless-smart-snippet.js';
export { buildSmartSnippet } from '../../../../controllers/smart-snippet/headless-smart-snippet.js';
/**
 * Defines a `SmartSnippet` controller instance.
 * @group Definers
 *
 * @param props - The configurable `SmartSnippet` properties.
 * @returns The `SmartSnippet` controller definition.
 * */
export function defineSmartSnippet(props) {
    return {
        build: (engine) => buildSmartSnippet(engine, props),
    };
}
