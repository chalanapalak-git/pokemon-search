import { buildBreadcrumbManager, } from '../../../../controllers/breadcrumb-manager/headless-breadcrumb-manager.js';
export { buildBreadcrumbManager } from '../../../../controllers/breadcrumb-manager/headless-breadcrumb-manager.js';
/**
 * Defines a `BreadcrumbManager` controller instance.
 * @group Definers
 *
 * @returns The `BreadcrumbManager` controller definition.
 * */
export function defineBreadcrumbManager() {
    return {
        build: (engine) => buildBreadcrumbManager(engine),
    };
}
