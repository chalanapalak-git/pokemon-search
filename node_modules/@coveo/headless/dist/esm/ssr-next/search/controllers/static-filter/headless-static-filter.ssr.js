import { buildStaticFilter, } from '../../../../controllers/static-filter/headless-static-filter.js';
export { buildStaticFilter, buildStaticFilterValue } from '../../../../controllers/static-filter/headless-static-filter.js';
/**
 * Defines a `StaticFilter` controller instance.
 * @group Definers
 *
 * @param props - The configurable `StaticFilter` properties.
 * @returns The `StaticFilter` controller definition.
 * */
export function defineStaticFilter(props) {
    return {
        build: (engine) => buildStaticFilter(engine, props),
    };
}
