import { buildDidYouMean, } from '../../../../controllers/did-you-mean/headless-did-you-mean.js';
export { buildDidYouMean } from '../../../../controllers/did-you-mean/headless-did-you-mean.js';
/**
 * Defines a `DidYouMean` controller instance.
 * @group Definers
 *
 * @returns The `DidYouMean` controller definition.
 * */
export function defineDidYouMean() {
    return {
        build: (engine) => buildDidYouMean(engine),
    };
}
