import { buildUrlManager, } from '../../../../controllers/url-manager/headless-url-manager.js';
import { MissingControllerProps } from '../../../common/errors.js';
export { buildUrlManager, initialStateSchema } from '../../../../controllers/url-manager/headless-url-manager.js';
/**
 * @internal
 * @group Definers
 *
 */
export const defineUrlManager = () => ({
    buildWithProps: (engine, props) => {
        // TODO: KIT-5154 - Apply commerce pattern
        if (props === undefined) {
            throw new MissingControllerProps('UrlManager');
        }
        return buildUrlManager(engine, { initialState: props.initialState });
    },
});
