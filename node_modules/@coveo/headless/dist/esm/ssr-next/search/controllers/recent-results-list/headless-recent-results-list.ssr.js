import { buildRecentResultsList, } from '../../../../controllers/recent-results-list/headless-recent-results-list.js';
export { buildRecentResultsList } from '../../../../controllers/recent-results-list/headless-recent-results-list.js';
/**
 * Defines a `RecentResultsList` controller instance.
 * @group Definers
 *
 * @param props - The configurable `RecentResultsList` properties.
 * @returns The `RecentResultsList` controller definition.
 * */
export function defineRecentResultsList(props) {
    return {
        build: (engine) => buildRecentResultsList(engine, props),
    };
}
