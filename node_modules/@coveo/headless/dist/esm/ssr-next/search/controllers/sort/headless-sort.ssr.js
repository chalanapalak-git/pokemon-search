import { buildSort, } from '../../../../controllers/sort/headless-sort.js';
export { buildSort } from '../../../../controllers/sort/headless-sort.js';
/**
 * Defines a `Sort` controller instance.
 * @group Definers
 *
 * @param props - The configurable `Sort` properties.
 * @returns The `Sort` controller definition.
 * */
export function defineSort(props) {
    return {
        build: (engine) => buildSort(engine, props),
    };
}
