import type { CommerceEngine } from '../app/commerce-engine/commerce-engine.js';
export declare namespace Search {
    const responseIdSelector: (engine: CommerceEngine) => string;
}
export declare namespace ProductListing {
    const responseIdSelector: (engine: CommerceEngine) => string;
}
