import type { InsightEngine } from '../../../app/insight-engine/insight-engine.js';
import { type Pager, type PagerInitialState, type PagerOptions, type PagerProps, type PagerState } from '../../core/pager/headless-core-pager.js';
export type { PagerInitialState, PagerOptions, PagerProps, Pager, PagerState };
/**
 * Creates an insight `Pager` controller instance.
 *
 * @param engine - The insight engine.
 * @param props - The configurable `Pager` properties.
 * @returns A `Pager` controller instance.
 *
 * @group Controllers
 * @category Pager
 * */
export declare function buildPager(engine: InsightEngine, props?: PagerProps): Pager;
