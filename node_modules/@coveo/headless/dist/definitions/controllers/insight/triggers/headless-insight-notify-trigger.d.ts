import type { InsightEngine } from '../../../app/insight-engine/insight-engine.js';
import { type NotifyTrigger, type NotifyTriggerState } from '../../core/triggers/headless-core-notify-trigger.js';
export type { NotifyTrigger, NotifyTriggerState };
/**
 * Creates an insight `NotifyTrigger` controller instance.
 *
 * @param engine - The insight engine.
 * @returns A `NotifyTrigger` controller instance.
 *
 * @group Controllers
 * @category NotifyTrigger
 * */
export declare function buildNotifyTrigger(engine: InsightEngine): NotifyTrigger;
