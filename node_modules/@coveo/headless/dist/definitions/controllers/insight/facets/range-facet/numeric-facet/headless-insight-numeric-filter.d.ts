import type { InsightEngine } from '../../../../../app/insight-engine/insight-engine.js';
import { type NumericFilter, type NumericFilterInitialState, type NumericFilterOptions, type NumericFilterProps, type NumericFilterRange, type NumericFilterState } from '../../../../core/facets/range-facet/numeric-facet/headless-core-numeric-filter.js';
export type { NumericFilterOptions, NumericFilterInitialState, NumericFilterRange, NumericFilterProps, NumericFilterState, NumericFilter, };
/**
 * Creates an insight `NumericFilter` controller instance.
 * @param engine - The insight engine.
 * @param props - The configurable `NumericFilter` controller properties.
 * @returns A `NumericFilter` controller instance.
 *
 * @group Controllers
 * @category NumericFilter
 */
export declare function buildNumericFilter(engine: InsightEngine, props: NumericFilterProps): NumericFilter;
