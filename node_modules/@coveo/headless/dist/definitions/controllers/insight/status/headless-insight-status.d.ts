import type { InsightEngine } from '../../../app/insight-engine/insight-engine.js';
import { type SearchStatus, type SearchStatusState } from '../../core/status/headless-core-status.js';
export type { SearchStatus, SearchStatusState };
/**
 * Creates an insight `SearchStatus` controller instance.
 *
 * @param engine - The insight engine.
 * @returns A `SearchStatus` controller instance.
 *
 * @group Controllers
 * @category SearchStatus
 */
export declare function buildSearchStatus(engine: InsightEngine): SearchStatus;
