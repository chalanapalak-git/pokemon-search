import type { Relay } from '@coveo/relay';
import type { Dispatch, Middleware, Reducer, ReducersMapObject, StateFromReducersMapObject, ThunkDispatch, UnknownAction, Unsubscribe } from '@reduxjs/toolkit';
import type { Logger } from 'pino';
import type { ConfigurationState, CoreConfigurationState } from '../features/configuration/configuration-state.js';
import type { SearchParametersState } from '../state/search-app-state.js';
import type { EngineConfiguration } from './engine-configuration.js';
import type { LoggerOptions } from './logger.js';
import { type NavigatorContext, type NavigatorContextProvider } from './navigator-context-provider.js';
import { stateKey } from './state-key.js';
import { type CoreExtraArguments, type Store } from './store.js';
import type { ThunkExtraArguments } from './thunk-extra-arguments.js';
export type CoreState<Configuration extends CoreConfigurationState = CoreConfigurationState> = {
    configuration: Configuration;
    version: string;
} & Partial<SearchParametersState>;
type EngineDispatch<State, ExtraArguments extends ThunkExtraArguments> = ThunkDispatch<State, ExtraArguments, UnknownAction> & Dispatch<UnknownAction>;
export interface CoreEngine<State extends object = {}, ExtraArguments extends ThunkExtraArguments = ThunkExtraArguments, Configuration extends CoreConfigurationState = CoreConfigurationState> {
    /**
     * Dispatches an action directly. This is the only way to trigger a state change.
     * Each headless controller dispatches its own actions.
     *
     * @param action An action supported by the headless engine.
     *
     * @returns For convenience, the action object that was just dispatched.
     */
    dispatch: EngineDispatch<State & CoreState<Configuration>, ExtraArguments>;
    /**
     * Adds a change listener. It will be called any time an action is
     * dispatched, and some part of the state tree may potentially have changed.
     * You may then access the new `state`.
     *
     * @param listener A callback to be invoked on every dispatch.
     * @returns A function to remove this change listener.
     */
    subscribe(listener: () => void): Unsubscribe;
    /**
     * The complete headless state tree.
     */
    state: State & CoreState<Configuration>;
    /**
     * The Relay instance used by Headless.
     */
    relay: Relay;
    /**
     * The redux store.
     */
    store: Store;
    /**
     * The logger instance used by headless.
     * */
    logger: Logger;
    /**
     * Adds the specified reducers to the store.
     *
     * @param reducers - An object containing the reducers to attach to the engine.
     */
    addReducers(reducers: ReducersMapObject): void;
    /**
     * Enable analytics tracking
     */
    enableAnalytics(): void;
    /**
     * Disable analytics tracking
     */
    disableAnalytics(): void;
    /**
     * The navigator context (referer, location, UserAgent)
     */
    navigatorContext: NavigatorContext;
}
export type CoreEngineNext<State extends object = {}, ExtraArguments extends ThunkExtraArguments = ThunkExtraArguments, Configuration extends CoreConfigurationState = CoreConfigurationState> = Omit<CoreEngine<State, ExtraArguments>, 'state' | 'store'> & {
    /**
     * The readonly internal state of the headless engine.
     *
     * @internal
     */
    readonly [stateKey]: State & CoreState<Configuration>;
    /**
     * The readonly global headless engine configuration
     */
    readonly configuration: Configuration;
};
export interface EngineOptions<Reducers extends ReducersMapObject> extends ExternalEngineOptions<StateFromReducersMapObject<Reducers>> {
    /**
     * Map object of reducers.
     * A reducer is a pure function that takes the previous state and an action, and returns the next state.
     * ```
     * (previousState, action) => nextState
     * ```
     * [Redux documentation on reducers.](https://redux.js.org/glossary#reducer)
     */
    reducers: Reducers;
    /**
     * An optional cross reducer (aka: root reducer) that can be combined with the slice reducers.
     *
     * [Redux documentation on root reducers.](https://redux.js.org/usage/structuring-reducers/beyond-combinereducers)
     */
    crossReducer?: Reducer;
}
export interface ExternalEngineOptions<State extends object> {
    /**
     * The global headless engine configuration options.
     */
    configuration: EngineConfiguration;
    /**
     * The initial headless state.
     * You may optionally specify it to hydrate the state
     * from the server in universal apps, or to restore a previously serialized
     * user session.
     */
    preloadedState?: State;
    /**
     * List of additional middlewares.
     * A middleware is a higher-order function that composes a dispatch function to return a new dispatch function.
     * It is useful for logging actions, performing side effects like routing, or turning an asynchronous API call into a series of synchronous actions.
     *
     * @example
     * ```
     * type MiddlewareAPI = { dispatch: Dispatch, getState: () => State }
     * type Middleware = (api: MiddlewareAPI) => (next: Dispatch) => Dispatch
     * ```
     * [Redux documentation on middlewares.](https://redux.js.org/glossary#middleware)
     */
    middlewares?: Middleware<{}, State>[];
    /**
     * The logger options.
     */
    loggerOptions?: LoggerOptions;
    /**
     * An optional function returning navigation context. (referer, location, UserAgent)
     */
    navigatorContextProvider?: NavigatorContextProvider;
}
export declare function buildEngine<Reducers extends ReducersMapObject, ExtraArguments extends ThunkExtraArguments>(options: EngineOptions<Reducers>, thunkExtraArguments: ExtraArguments): CoreEngine<StateFromReducersMapObject<Reducers>, CoreExtraArguments & ExtraArguments, ConfigurationState>;
export declare function buildCoreEngine<Reducers extends ReducersMapObject, ExtraArguments extends ThunkExtraArguments, Configuration extends CoreConfigurationState>(options: EngineOptions<Reducers>, thunkExtraArguments: ExtraArguments, configurationReducer: Reducer<Configuration>): CoreEngine<StateFromReducersMapObject<Reducers>, ExtraArguments, Configuration>;
export declare const nextAnalyticsUsageWithServiceFeatureWarning: string;
export declare function warnIfUsingNextAnalyticsModeForServiceFeature(analyticsMode: 'next' | 'legacy'): void;
export {};
