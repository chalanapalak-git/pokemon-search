import React, { useState, FormEvent } from 'react'; import { buildSearchBox } from '@coveo/headless'; import { engine } from './engine'; const searchBoxController = buildSearchBox(engine); export function SearchBox() { const [query, setQuery] = useState(''); const handleSubmit = (e: FormEvent) => { e.preventDefault(); searchBoxController.updateText(query); searchBoxController.submit(); }; return ( <form onSubmit={handleSubmit}> <input type="text" placeholder="Search Pokémon..." value={query} onChange={(e) => setQuery(e.target.value)} style={{ padding: '5px', width: '200px' }} /> <button type="submit" style={{ marginLeft: '5px' }}> Search </button> </form> ); }